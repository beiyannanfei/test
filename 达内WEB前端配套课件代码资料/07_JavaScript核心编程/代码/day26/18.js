var regexp = /is/g;		//全局匹配，regexp.lastIndex属性生效

var data = 'His history is long';

console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
//regexp.lastIndex = 14;
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );
console.log( regexp.lastIndex );	//下一次搜索从哪里开始
console.log( regexp.test(data) );