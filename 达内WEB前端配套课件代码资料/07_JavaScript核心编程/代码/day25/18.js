/*
子串的替换
*/
var data = 
'Microsoft microsoft’s MICROSOFT microsoft';

var result = data.replace('microsoft','oracle');

console.log( data );
console.log( result );

var msg = "<hr/><hr/>";
document.write( msg );
msg.replace('<', '&lt;');
msg.replace('>', '&gt;');
